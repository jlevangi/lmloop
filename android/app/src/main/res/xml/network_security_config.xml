<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Project previews may use explicit private-LAN HTTP URLs. Keep the
         dashboard itself HTTPS; replace this exception when previews gain a
         stable TLS endpoint. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
